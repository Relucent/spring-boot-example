<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="yyl.springboot.mapper.db2.Hello2Mapper">

	<resultMap id="RESULT_MAP" type="yyl.springboot.entity.db2.Hello2">
		<id column="id" property="id" jdbcType="BIGINT" />
		<result column="name" property="name" jdbcType="VARCHAR" />
		<result column="value" property="value" jdbcType="VARCHAR" />
	</resultMap>

	<sql id="TABLE">hello2</sql>

	<sql id="COLUMNS">
		id, name, value
	</sql>

	<select id="findAll" resultMap="RESULT_MAP">
		SELECT
		<include refid="COLUMNS" />
		FROM
		<include refid="TABLE" />
	</select>

	<select id="getById" parameterType="java.lang.Long" resultMap="RESULT_MAP">
		SELECT
		<include refid="COLUMNS" />
		FROM
		<include refid="TABLE" />
		WHERE id = #{id}
	</select>

	<insert id="insert" parameterType="yyl.springboot.entity.db2.Hello2">
		INSERT INTO
		<include refid="TABLE" />
		(name,value)
		VALUES
		(#{name}, #{value})
	</insert>

	<update id="update" parameterType="yyl.springboot.entity.db2.Hello2">
		UPDATE
		<include refid="TABLE" />
		<set>
			<if test="name != null">
				name = #{name},
			</if>
			<if test="value != null">
				value = #{value},
			</if>
		</set>
		WHERE
		id = #{id}
	</update>

	<delete id="deleteById" parameterType="java.lang.Long">
		DELETE FROM
		<include refid="TABLE" />
		WHERE
		id =#{id}
	</delete>

</mapper>